export interface I{{component_name}} {
  sampleTextProp: string;
}

const {{component_name}}: React.FC<I{{component_name}}> = ({ sampleTextProp }) => {
  return <div className="">{sampleTextProp} </div>;
};

export default {{component_name}};